#include "main.h"

/**
 * _puts - a function that prints a str followed by a new line
 * @str: the string
 *
 * Return: string
 */
void _puts(char *str)
{
	int len = 0;

	while (str[len])
	_puts("%d\n", str);
	len++;
return (str);
}
