#include "main.h"

/**
 * puts2 - prints extras of a string
 * @str: the string used
 * Return: void
 */
void puts2(char *str)
{
	int i;

	for (i = 0; i <= str[i]; i++)
{
	if (i % 2 == 0)
{
	_putchar(str[i]);
}
}
_putchar('\n');
}
