#include "main.h"

/**
 * mul - multiplies two integers
 * @a: number to multiply
 * @b: number to multiply
 *
 * Return: always 0
 */
int mul(int a, int b)
{
int result;

result = a * b;
return (result);
}
