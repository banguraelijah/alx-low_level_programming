#include <stdio.h>
/**
 * main - runs the code when executed
 *
 * Return: always 0 (success)
 */
int n = 48;
int letter = 'a';
while (n <= 57)
{
putchar((num % 10) + '0');
n++;
}
while (letter <= 'f')
{
putchar(letter);
letter++;
}
putchar('\n');
return (0);
}
