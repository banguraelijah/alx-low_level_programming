#include <stdio.h>
/**
 * main - runs the code when ever it is excuted
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int x = 'a';
while (x <= 'z')
{
putchar(x);
putchar("\n");
x++;
}
return (0);
}
