#include "main.h"
/**
 * _islower - checks for lower case characters
 *
 * Return: 1 if c is lowercase
 * Return: 0 otherwise
 */
int _islower(int c)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
{
	if (letter == islower)
{
	return (1);
}
else
{
	return (0);
}
}
}
