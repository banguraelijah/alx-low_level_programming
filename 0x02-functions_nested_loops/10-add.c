#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result
 * @a: number for addition
 * @b: number for addition
 * Return: result
 */
int add(int a, int b);
{
	result = a + b;
return (result);
}
